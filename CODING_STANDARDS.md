# Coding Standards

Controllers must remain thin.

Business logic belongs to Actions.

Validation belongs to Form Requests.

Authorization belongs to Policies.

Services are allowed only for reusable domain logic.

Prefer Laravel conventions over custom abstractions.